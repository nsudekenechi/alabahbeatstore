<!DOCTYPE html>
<html>
  <head>
    <style>
      body { font-family: sans-serif; }
      .btn { background: #000; color: white; padding: 10px 20px; text-decoration: none; }
      img{
        width: 100px;
        height: 100px;
      }
    </style>
  </head>
  <body>
<div>
  <h2>Hi <%= username %>,</h2>
    <%- body %>
</div>
  </body>
</html>
